{
  "name": "invompt",
  "version": "0.10.3",
  "description": "Create and manage professional invoices, quotes, estimates, and pro formas from natural-language requests in any language.",
  "mcpServers": {
    "invompt": {
      "command": "node",
      "args": [
        "${extensionPath}${/}dist${/}index.js"
      ],
      "cwd": "${extensionPath}"
    }
  },
  "settings": [
    {
      "name": "Invompt guest credential",
      "description": "Optional server-issued guest credential shared through INVOMPT_GUEST_CREDENTIAL.",
      "envVar": "INVOMPT_GUEST_CREDENTIAL",
      "sensitive": true
    }
  ]
}
