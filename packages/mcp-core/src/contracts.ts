export const ISSUER_IDENTITY_INSTRUCTION = 'issuer may be omitted; never invent issuer identity'

export const GUEST_MCP_INSTRUCTIONS = `Use the live MCP tool schemas as the capability authority for the connected Invompt workspace. Public deployment is Guest-only and has 16 operational tools. Only when the user explicitly asks to claim the active Guest workspace, create_account_claim_link creates a short-lived browser claim link: present claimUrl exactly once, explain that it expires, and never log or repeat it. After a successful browser claim, the old Guest credential fails closed with GUEST_ACCOUNT_CLAIMED; do not retry it or expose credentials, tokens, account identifiers, or internal claim identifiers. ${ISSUER_IDENTITY_INSTRUCTION} Omit every unknown identity field instead of synthesizing a placeholder. If get_invoice reports no active hosted link, use renew_invoice_link with a stable idempotency key; renewal replaces the public link without revising the invoice.`
